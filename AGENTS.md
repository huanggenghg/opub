# 项目协作约定

开发前阅读 `CLAUDE.md`，遵守其中的项目约束与验证要求。

## 修复后的默认交付流程

用户已于 2026-09-14 授权：以后本项目的修复，默认完成验证、提交、推送和必要的 PyPI 发布，无需重复询问。

- 修复后补充相关回归验证；测试通过后提交并推送到远端主分支。
- 如果修复影响已发布 Python 包，递增补丁版本，同步项目、锁文件、技能元数据和版本测试，构建并发布到 PyPI，推送对应版本标签。
- 发布前确认远端 CI 通过，发布后确认 PyPI 版本及安装结果。
- 仅提交本次任务相关改动，保留用户其他未提交或未跟踪文件。
- 验证失败或发布受阻时，修复问题或如实报告阻碍，不把未完成的发布报告为成功。
