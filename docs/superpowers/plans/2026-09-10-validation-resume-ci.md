# 发布校验、恢复与 CI 实施计划

**Goal:** 完成剩余三项可靠性优化。

**Architecture:** validation.py 负责无副作用校验，history.py 负责独立 SQLite 记录与原子状态迁移，编排器负责准备输入、恢复与结果输出。

**Tech Stack:** Python >=3.9、SQLite、pytest、GitHub Actions。

- [x] 校验：添加无浏览器的输入失败测试；实现平台能力/素材/定时校验与 dry-run，先解析所有标题再发布。
- [x] 恢复：先测试 SQLite 状态、素材指纹、原子认领及未知结果保护，再接入 --resume 和 run_id 输出。
- [x] CI：消除既有慢测试等待，添加跨版本测试、构建和独立安装验证。
- [x] 集成：更新 CLI/技能说明，执行完整回归，检查产物排除运行记录和用户数据。

每步先验证测试失败，再实现并运行相关回归；最后整体验证，不执行真实发布。

## 验证记录（2026-09-13）

- 干净源码目录（不含本机账号、素材、密钥或发布记录），Python 3.12 全量测试：725 passed，72 subtests passed，41.44 秒；无排除测试。仅既有 Starlette/httpx 弃用提示。
- wheel 与 sdist 构建成功；检查新增模块齐全，产物不含账号、发布记录、密钥或许可服务代码。
- 独立 Python 3.9.6 / 3.12.13 环境安装 wheel，离开源码目录执行帮助、版本和失败预检 JSON 检查均通过。
- `uv lock --check`、`git diff --check`、CI YAML 结构检查通过。
- 验证覆盖：全部标题先准备、错误输入不登录、成功项复用、安全失败恢复、不明结果阻止重发、并发状态变化、素材指纹改变、保存失败立即停止及 dry-run 无发布副作用。
- 文档明确 `--start-from` 是新任务目录序号，`--resume` 才复用平台结果；当前 B站定时在入口拦截。
- GitHub Actions 配置已添加，远端运行须由后续推送触发；本次未执行真实平台发布、付费激活、提交、推送或版本发布。

Python 3.9 扩展验证发现旧许可库存测试依赖 WAL 文件生命周期。已在测试中保留读事务直到完成 WAL 明文检查，再释放事务验证 checkpoint 后的主库；保留全部安全断言。相关 21 项测试在 Python 3.9 / 3.12 均通过，应用逻辑未修改。

最终 Python 3.9.6 干净源码全量回归：725 passed，54.69 秒，无排除测试。该系统 Python 的 urllib3/LibreSSL 环境提示保留；Python 3.12 全量与两版本独立 wheel 安装验证见上文。
